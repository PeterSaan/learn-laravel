# learn-laravel
- Make sure `.env` file is present
- Run `docker compose up` for mariadb
- Install stuff:
```bash
composer i
npm i
```
- Run the app:
```bash
php artisan key:generate
php artisan migrate
php artisan serve
```
